{
  "name": "fishtank-userscript",
  "version": "5.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && vite build --mode unminified",
    "build:test": "vite build --mode testing",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix"
  },
  "devDependencies": {
    "eslint": "^10.1.0",
    "terser": "^5",
    "vite": "^7.3.2",
    "vite-plugin-monkey": "^7.1.9"
  }
}
